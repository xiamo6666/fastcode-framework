plugins {
    id 'org.springframework.boot' version "${springBootVersion}"
}
dependencies {
    api project(':fc-core')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}
bootJar {
    mainClass.set('com.fc.system.SystemApplication')
}
